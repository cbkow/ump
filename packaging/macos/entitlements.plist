<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened runtime entitlements for DMG distribution -->

    <!-- Allow loading vendored dylibs (FFmpeg, OCIO, etc.) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow loading dylibs from @rpath (Frameworks/) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network access for Frame.io integration -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- File access for media playback (user-selected files) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
</dict>
</plist>
